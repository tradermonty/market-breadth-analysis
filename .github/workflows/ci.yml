name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    paths:
      - '**.py'
      - 'pyproject.toml'
      - '.pre-commit-config.yaml'
      - 'requirements.txt'

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install tools
        run: pip install ruff codespell tomli mypy pandas-stubs types-requests types-tqdm

      - name: Ruff lint
        run: ruff check .

      - name: Ruff format check
        run: ruff format --check .

      - name: Mypy type check
        run: mypy --config-file pyproject.toml

      - name: Codespell
        run: codespell

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install tools
        run: pip install bandit pip-audit detect-secrets

      - name: Bandit (SAST)
        run: bandit -c pyproject.toml -r . --exclude archive,venv311,tests

      - name: pip-audit
        run: pip-audit -r requirements.txt || true

      - name: Detect secrets
        run: >-
          detect-secrets scan
          --exclude-files 'venv311/.*'
          --exclude-files 'archive/.*'
          --exclude-files 'data/.*'
          --list-all-secrets
          && echo "No new secrets detected"

  test:
    name: Tests
    runs-on: ubuntu-latest
    env:
      SKIP_API_TESTS: 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt --ignore-installed PyQt5

      - name: Run tests
        run: python -m pytest tests/ -v --tb=short
